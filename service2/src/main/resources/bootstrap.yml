server:
  port: 8082

spring:
  application:
    name: service2
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config
eureka:
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://127.0.0.1:8761/eureka/